# Generierte Audio-Artefakte (reproduzierbar aus JSON + Synthese-Skripten)
consecutive_reading/bbox/**/*.wav
consecutive_reading/bbox/**/*.mp3
*.wav
*.mp3

# Python-Cache
__pycache__/
*.pyc
*.pyo
*.pyd
.python-version

# PyTorch-Modelle — bleiben im Repo (klein, wichtig für Reproduzierbarkeit)
# Nur sehr große Modelle (>10 MB) sollten ggf. via Git LFS ausgelagert werden
# *.pth
# *.pt

# IDE / Editor
.vscode/
.idea/
*.swp
*~

# OS
.DS_Store
Thumbs.db

# Temporäre Backups
*.bak
*.tmp
