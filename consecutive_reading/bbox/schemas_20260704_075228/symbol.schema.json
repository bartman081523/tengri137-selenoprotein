{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": ["symbol_index", "page", "first_appearance", "bbox", "kind", "description"],
  "properties": {
    "symbol_index": {
      "type": "integer",
      "minimum": 1,
      "description": "Global index, ordered by first appearance in flow text, top-left to bottom-right reading order, page 1 first."
    },
    "page": {"type": "string", "pattern": "^p\\d{2}$"},
    "first_appearance": {"type": "boolean"},
    "bbox": {
      "type": "array",
      "items": {"type": "integer"},
      "minItems": 4,
      "maxItems": 4
    },
    "kind": {"type": "string", "description": "Family, e.g. chinese_oracle_script, geometric_diamond_with_dot, math_pi, latin_print, red_label"},
    "unicode_codepoint": {"type": "string", "description": "Unicode codepoint, e.g. U+5929"},
    "description": {"type": "string"},
    "vision_verified": {"type": "boolean", "default": false},
    "geometry_ref": {"type": "string", "description": "ID of the reusable geometry object in symbols_index.json"}
  }
}
